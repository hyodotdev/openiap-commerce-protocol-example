The final clean-source desktop walkthrough showed stale wording after delivery: the message still said the change was queued while the counter was zero. Removed that clause; the queue and inbox counters report delivery status. The HTTP trace was moved into an optional disclosure so the outcome is visible without reading JSON. The screenshot tool produced a scaled full-page capture; only normal viewport captures were retained.
